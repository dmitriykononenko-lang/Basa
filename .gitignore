# PHP / Composer
server/vendor/
server/.env

# SSL certificates (never commit private keys)
docker/nginx/ssl/*.pem
docker/nginx/ssl/*.crt
docker/nginx/ssl/*.key

# Storage runtime data
storage/

# Widget build artifacts
dist/
*.zip

# OS
.DS_Store
Thumbs.db

# backups / dumps — никогда не коммитить
*.dump
*.dump.age
*.sql.gz
basa-backup-*/
backup/
cron_jobs_*.txt
