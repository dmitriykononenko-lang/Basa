-- ============================================================
-- 0029_counterparty_agent_enum: тип контрагента 'agent'
-- ============================================================
-- Отдельной миграцией ДО использования: новое значение enum нельзя
-- добавить и использовать в одной транзакции.

alter type public.counterparty_kind add value if not exists 'agent';
