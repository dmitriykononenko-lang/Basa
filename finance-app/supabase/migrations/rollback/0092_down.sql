-- Откат 0092. Удаляет канонический слой управленческой аналитики.
-- ВНИМАНИЕ: после отката страницы проекта, сотрудника, контрагента, бюджеты,
-- дашборд и отчёты перестанут работать — новый код читает это представление.
-- Поэтому откат 0092 делается ТОЛЬКО вместе с откатом приложения (SPLIT-01).
-- Данные при этом не затрагиваются: представление не хранит строк.
begin;
drop view if exists public.transaction_lines;
commit;
