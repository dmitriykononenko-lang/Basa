-- ============================================================
-- 0017_counterparty_employee_enum: значение 'employee' для типа контрагента
-- ============================================================
-- Добавляется отдельной миграцией ДО использования (объединение сотрудников в 0018),
-- т.к. новое значение enum нельзя добавить и использовать в одной транзакции.

alter type public.counterparty_kind add value if not exists 'employee';
