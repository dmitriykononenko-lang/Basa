-- ============================================================
-- 0068_kb_media_no_listing: убрать анонимный листинг файлов бакета kb-media.
--
-- kb-media — публичный бакет: объекты отдаются по прямому public-URL в обход RLS,
-- поэтому широкая SELECT-политика для доступа к файлам не нужна. Она лишь
-- позволяла перечислять все пути в бакете. Приложение метод .list() не использует
-- (только upload + getPublicUrl), поэтому удаление политики ничего не ломает.
-- ============================================================

drop policy if exists kb_media_select on storage.objects;
