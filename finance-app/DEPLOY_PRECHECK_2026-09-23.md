# DEPLOY_PRECHECK — PR #99, 2026-09-23

Шаг §1 `PRODUCTION_DEPLOY_RUNBOOK.md`. **Все проверки только на чтение.
Production не изменён:** миграции не применялись, код не деплоился, cron не
трогался, данные не менялись.

## 1. Состояние ветки

`claude/intelligent-ramanujan-Jdbbu`, 9 коммитов впереди `origin/main`, PR #99.
Последний — `f2509b1` (FX design freeze; FX-миграций и FX-кода в PR нет).

## 2. Тесты и сборка

| Проверка | Результат |
|---|---|
| T1–T14, режим `pre` (схема как в проде) | **FAIL 12** — проблемы воспроизводятся, как и ожидается |
| T1–T14, режим `post` (0086–0092) | **PASS 14 / FAIL 0** |
| Авторизация RPC | **PASS 21 / FAIL 0** |
| Разнесение операции (SPLIT) | **PASS 36 / FAIL 0** |
| Репетиция миграций на объёме прода | 7 миграций за **0,27 с**; `transactions` перезаписана, размер 2 280 → 5 680 kB; целостность после миграций чистая; T1–T14 и AUTHZ на том же наборе зелёные |
| `tsc --noEmit` | без ошибок |
| `next build` | Compiled successfully |

## 3. Базовая линия целостности production

**24 PASS + 2 известных FAIL** — ровно как задокументировано:

| Проверка | Значение | Статус |
|---|---|---|
| `double-counted transfers (FIN-03)` | **126** | известный, вне объёма PR #99 |
| `dup auto accrual(cp,type,part,month)` | **1** | известный: ручное начисление рядом с авто, не гонка |
| остальные 24 проверки | 0 | PASS |

## 4. Контрольные цифры «до»

| Показатель | Значение |
|---|---|
| операций всего / actual / planned | 7 959 / 7 951 / 8 |
| обязательств / разнесений | 190 / 96 |
| инвойсов / позиций | 25 / 22 |
| частей операций | 4 |
| счетов / контрагентов / батчей импорта | 24 / 158 / 269 |
| чистый поток RUB (actual, минор) | −10 198 192 |
| чистый поток USDT (actual, минор) | 305 451 |
| сумма `opening_balance` (минор) | 61 904 483 |
| последний импорт | 2026-09-23 06:04:03 UTC |
| последний синк Точки | 2026-09-23 06:04:07 UTC |

`opening_balance` по каждому счёту зафиксирован в этом файле ниже — в ходе
деплоя PR #99 он **не меняется** (это тема FIN-03, отдельный этап).

```
40802810520000183872=350087; Прочие платежи=-5510000; Счет Лицензии=95483971;
Счет Услуги=69776639; Фонд Налоги=-7897135; Фонд Обучение=-3963723;
Фонд премии Лицензии=-1587380; Фонд Прибыль=-34194772; Фонд Продвижение=-17991435;
Фонд Развитие=-4827048; Фонд Резерв=-3683223; Фонд Сервисы и лицензии=16710185;
Фонд Технической стабильности=625000; Фонд ФОТ Команда=-41386683
```

## 5. Артефакт для отката 0087

`supabase/migrations/rollback/0087_functions_before.sql` — тела
`materialize_auto_accruals(uuid)` и `materialize_support_cycles(uuid)`, снятые с
production через `pg_get_functiondef()`. Файл проверен: применяется без ошибок.
Порядок отката 0087: сначала этот файл, затем `0087_down.sql`.

## 6. Планировщики, которые надо отключить на время миграций

| Задача | Расписание | Статус |
|---|---|---|
| `tochka-autosync` (pg_cron) | `0 */3 * * *` | **active** |
| `bybit-sync-daily` (pg_cron) | `0 */3 * * *` | **active** |
| Vercel cron | `vercel.json`: 05:00, 06:00, 07:00, 08:00 | активны |

Последний прогон был в 06:04 UTC — следующий в ближайшие :00 трёхчасового слота.
Окно миграций нужно выбирать не вплотную к нему.

## 7. Вывод

PRECHECK **пройден полностью**. Блокеров нет. Готово к шагам §2–§4 runbook
(отключение cron → PITR-точка → миграции), которые требуют отдельного
подтверждения владельца: это первые необратимые действия на production.
