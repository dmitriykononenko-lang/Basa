# PRE_PRODUCTION_REVIEW.md — ревью PR #99 перед production

Дата 2026-09-22. **К production не применялось ничего**: миграции не запускались,
исторические данные не изменялись, 252 строки FIN-03 не удалялись, `opening_balance`
не менялся. Все измерения на production — только `SELECT`.

Воспроизведение всех проверок (production не используется):

```bash
cd finance-app
bash scripts/concurrency/run.sh          # T1–T14, режимы pre и post
MODE=post bash scripts/concurrency/authz_test.sh    # авторизация RPC
MODE=post bash scripts/concurrency/split_test.sh    # бизнес-сценарий разнесения
bash scripts/concurrency/split_prod_replay.sh      # BEFORE/AFTER на данных production
bash scripts/concurrency/rehearsal.sh    # репетиция миграций на объёме прода
```

Итоги прогонов 2026-09-22:

| Набор | pre (как в production) | post (с миграциями 0086–0092) |
|---|---|---|
| Конкурентность и импорт, T1–T14 | PASS 2 / **FAIL 12** | **PASS 14** / FAIL 0 |
| Авторизация RPC (21 проверка) | — | **PASS 21** / FAIL 0 |
| Разнесение операции (36 проверок) | PASS 26 / **FAIL 2** | **PASS 36** / FAIL 0 |
| Репетиция миграций на объёме прода | — | 7 миграций за 0,64 с, регрессия и авторизация на том же наборе зелёные |
| BEFORE/AFTER на реальных данных прода | — | 2 операции / 4 части: см. §6 |

---

## 1. Bank event identity — блокер подтверждён и устранён

### Что показал collision analysis на всей истории production

Отпечаток первой версии 0090 — `team|account|date|amount|currency|direction`:

| Метрика | Значение |
|---|---|
| Групп отпечатка среди банковских строк | 6 931 |
| **Групп с коллизией** | **537** |
| Строк, попавших в коллизии | 1 164 |
| «Лишних» строк, которые правило кратности могло пропустить | **627** |
| **Групп, где 2+ РАЗНЫХ `provider transaction id`** | **446** |
| Групп, различающихся только назначением платежа | 368 |
| Групп, различающихся только контрагентом | 20 |

446 групп — это заведомо **разные реальные операции** (у них разные идентификаторы
в банке), которые первая версия правила кратности при следующем импорте могла
молча пропустить. Это silent loss финансовой операции → первая версия 0090 была
**NO-GO**, и она переписана.

Усиление отпечатка контрагентом и назначением платежа проблему не снимает:

| Отпечаток | Групп с коллизией | Из них с 2+ provider id |
|---|---|---|
| + контрагент + первые 40 симв. назначения | 238 | 112 |
| + первые 80 | 71 | 71 |
| + первые 120 | 5 | 5 |
| + ПОЛНОЕ назначение | 2 | **2** |
| + контрагент + нормализованное назначение (итоговая формула) | 201 | **75** |

Остаются **75 групп**, неразличимых даже по полному описанию. Пример из
production: два вывода Bybit 2026-05-13 на 100,00 USDT с одинаковым текстом
«Bybit вывод USDT» и разными id `bybit-wd-226074267` / `bybit-wd-226100544`.

**Вывод: инъективного отпечатка на этих данных не существует.** Отпечаток не может
быть основанием для пропуска операции, у которой есть идентификатор провайдера.

### Реализованный приоритет идентичности (0090 v2)

1. `bank_event_id` = `provider : provider account : provider transaction id`
   (generated column, уникален в команде) — **единственный** признак, по которому
   кандидат с id провайдера может быть пропущен.
2. `bank_event_fp_strong` = счёт · дата · сумма · валюта · направление · контрагент ·
   **нормализованное** назначение — только для кросс-импортной сверки.
   Нормализация (`bank_note_norm`): отрезать хвост после `·`
   («Платежное поручение №…»), оставить буквы и цифры, привести к нижнему регистру.
   Замер на production: даёт совпадение **130 из 130** известных кросс-импортных
   пар FIN-03 — то есть достаточна для сверки без произвольной длины префикса.
3. `bank_event_fp` (слабый) — для индексирования и обнаружения неоднозначностей.

Правила `bank_import_commit`:

| # | Условие | Действие |
|---|---|---|
| 1 | есть `bank_event_id`, такой уже есть в БД | пропуск (дубль по id провайдера) |
| 2 | есть `bank_event_id`; ровно 1 кандидат и ровно 1 существующая строка без id провайдера с тем же сильным отпечатком | **повышение** существующей строки до канонической банковской |
| 3 | есть `bank_event_id`, сопоставление не 1:1 | **вставка** + запись в журнал сверки (`ambiguous_match`) |
| 4 | нет `bank_event_id` (CSV); ровно 1 кандидат и ровно 1 существующая строка | пропуск + запись в журнал (`auto_merged`) |
| 5 | нет `bank_event_id`, совпадений нет | вставка |
| 6 | нет `bank_event_id`, сопоставление не 1:1 | **вставка** + запись в журнал (`ambiguous_duplicate`) |

Ни одна операция не удаляется и не пропускается молча: любой пропуск по
отпечатку журналируется в `bank_reconciliation_conflicts` вместе с полным
payload кандидата, поэтому решение видно и обратимо. Неоднозначность никогда
не приводит к автоматическому склеиванию — только к вставке и конфликту.

### Требуемые регрессионные тесты — добавлены

| Тест | Сценарий | pre | post |
|---|---|---|---|
| T12 | две **разные** операции: тот же счёт/дата/сумма, но разные назначение и контрагент | PASS (дедупа нет вообще) | **PASS** — обе сохранены |
| T13 | одно событие из CSV и из Точки, описание различается служебным хвостом | FAIL — 2 строки, оборот 15 400 | **PASS** — 1 строка, оборот 7 700 |
| T14 | два разных события провайдера с **полностью совпадающим** отпечатком, двумя вызовами импорта | PASS | **PASS** — обе сохранены + заведён conflict |

T14 — прямая проверка того самого сценария silent drop из collision analysis.

---

## 2. T8 — все пути записи в `transactions`

Инвентаризация: 21 место в коде + 5 функций БД. Классификация и обоснование:

| Путь | Класс | Как обеспечена безопасность | Статус |
|---|---|---|---|
| `OperationCard` сохранение | USER EDIT | `transaction_save` + CAS по `version`; части в той же транзакции | ✅ |
| `OperationCard` удаление | USER EDIT | `transaction_delete` + CAS | ✅ |
| `PlannedReview` правка даты / проведение | USER EDIT | `transaction_save` + CAS | ✅ |
| `PlannedReview` удаление одной | USER EDIT | `transaction_delete` + CAS | ✅ |
| `PlannedReview` авто-закрытие сматченных | RECONCILIATION | удаление плановых строк, выбранных тем же клиентом; идемпотентно (повторный проход находит 0) | ⚠ документировано |
| `OperationsTable` массовая правка | USER EDIT bulk | `transactions_bulk_patch` — whitelist полей (сумму/дату/тип/валюту изменить нельзя), право на каждую строку, `version` двигается | ✅ |
| `OperationsTable` массовое удаление | USER EDIT bulk | явный выбор строк пользователем; удаление идемпотентно; RLS по строкам | ⚠ документировано |
| `OperationsTable` склейка в перевод (3 места) | RECONCILIATION | `transactions_merge_transfer` / `transaction_match_planned` — одна транзакция | ✅ |
| `CategorizeWizard` (4 места), `UncategorizedSorter` | USER EDIT | `transactions_bulk_patch` | ✅ |
| `RulesManager` применение правила | SYSTEM DERIVED bulk | было `update` по фильтру без списка id; теперь набор id фиксируется `select`, затем `transactions_bulk_patch` / `transactions_convert_to_transfer` | ✅ |
| `ImportWizard` реконсиляция встречных | RECONCILIATION | `transactions_convert_to_transfer` (проверка прав, принадлежность счёта, `version`) | ✅ |
| `ImportWizard` / `StatementImportWizard` вставка | IMPORT | `bank_import_commit` — одна транзакция, идентичность события | ✅ |
| `tochka-import` (ручной, cron, авто-синк) | IMPORT | `bank_import_commit` | ✅ |
| `AgentPayouts` выплата, `PayrollRowActions` выплата | USER COMMAND (деньги) | `obligation_pay` — операция и разнесение одной транзакцией; ключ идемпотентности = id обязательства | ✅ |
| `PayrollRowActions` планирование | USER COMMAND | `transaction_insert` + ключ идемпотентности; в БД `transactions_obl_planned_uniq` | ✅ |
| `AddTransactionForm` создание | USER EDIT | `transaction_insert` + ключ идемпотентности на попытку отправки | ✅ |
| `PlanObligationButton` плановый платёж | USER COMMAND | уникальный индекс `transactions_obl_planned_uniq (obligation_id) where status='planned'` — дубль невозможен | ✅ по индексу |
| `RecurringManager` материализация | SYSTEM DERIVED | новый `transactions_recurring_slot_uniq (recurring_rule_id, occurred_on) where status='planned'`: гонка даёт 23505 вместо дублей, UI показывает понятное сообщение | ✅ по индексу |
| `bybit_ingest` (БД, cron) | IMPORT | EXECUTE только у `service_role`; вся функция — одна транзакция. Дедуп `not exists` без уникального индекса; **жёстко прошитые team/account** | ⚠ остаточный риск, вне объёма PR |
| `bybit_apply_parties` (БД, cron) | SYSTEM DERIVED | один `UPDATE`, идемпотентен | ✅ |
| `merge_counterparties` (БД) | SYSTEM DERIVED | набор `UPDATE ... where counterparty_id = dup` идемпотентен; повторный вызов — no-op | ✅ |
| `support_open_period` / `support_delete_period` (БД) | SYSTEM DERIVED | гонка «посчитал периоды → вставил» закрыта индексом `project_periods_project_month_uniq`; защита прав исправлена (см. §3) | ✅ |

Итог: **T8 закрыт полностью** для правок пользователя и массовых операций;
три оставшихся прямых пути (`PlannedReview` авто-закрытие, массовое удаление,
`bybit_ingest`) документированы с обоснованием, почему они не дают lost update.

---

## 3. Авторизация SECURITY DEFINER функций

### SEC-008 (HIGH) — найдено при этом ревью, существует в production сейчас

`can_edit_finance() / can_write_tx() / can_manage_team()` возвращают **NULL** для
того, кто не состоит в команде (`role in (...)` при `role IS NULL`). Для RLS это
отказ. Но в plpgsql `if not can_edit_finance(t) then raise` при NULL **не
срабатывает** — защита молча пропускает вызывающего.

Проверено на production (только чтение): `auth.uid() = NULL` →
`can_edit_finance(team) = NULL`, `(not can_edit_finance(team)) IS NULL = true`.

Такой guard стоит в **6** функциях: `support_open_period`, `support_delete_period`,
`merge_counterparties`, `materialize_auto_accruals`, `materialize_support_cycles`,
`academy_assign`. При этом `support_open_period` и `support_delete_period` имеют
EXECUTE у **PUBLIC и anon**, а они пишут в `project_periods`, `obligations` и
`transactions`.

Эмпирическое подтверждение (disposable DB, режим `pre`): пользователь, не
состоящий ни в одной команде, вызвал `support_open_period` для проекта чужой
команды — вызов **прошёл** и создал период (1 → 2). В режиме `post` тот же вызов
получает «Недостаточно прав», данные не меняются.

Исправление (0091) сделано в одном месте — в самих хелперах (`exists(...)` вместо
`role in (...)`), поэтому защищены все вызывающие, включая ещё не написанные.
Дополнительно сняты EXECUTE у anon/PUBLIC с пяти функций, которые пишут данные
(проверено grep'ом: приложение их из анонимного контекста не вызывает).

### Матрица проверки новых RPC

Для каждой новой функции: `SECURITY DEFINER` + `set search_path = public`,
EXECUTE только `authenticated` (у `is_service_role` — плюс `service_role`),
`op_begin`/`op_finish` клиенту не выданы вовсе.

| RPC | Кто может вызвать | Проверка команды | Проверка роли | Исключение service_role |
|---|---|---|---|---|
| `invoice_save` | authenticated | команда берётся у существующего инвойса, иначе из payload; контрагент/проект проверяются на принадлежность | `can_edit_finance` | нет |
| `obligation_allocate` | authenticated | команда из обязательства; операция проверяется на ту же команду | `can_edit_finance` | нет |
| `transaction_split` / `transaction_save` / `transaction_delete` | authenticated | команда из самой операции | `can_modify_tx` (финансовые роли или employee-владелец) | нет |
| `transactions_merge_transfer` / `transaction_match_planned` | authenticated | обе операции обязаны быть одной команды | `can_edit_finance` | нет |
| `transactions_bulk_patch` | authenticated | все id обязаны быть одной команды; справочники проверяются | `can_write_tx`; для `account_id`/`status` — `can_edit_finance`; плюс `can_modify_tx` на каждую строку | нет |
| `transactions_convert_to_transfer` | authenticated | все id одной команды; счёт проверяется | `can_edit_finance` | нет |
| `transaction_insert` | authenticated | `team_id` из payload проверяется по ролям; все ссылки — на принадлежность | `can_write_tx` | нет |
| `obligation_pay` | authenticated | команда из обязательства; счёт проверяется | `can_edit_finance` | нет |
| `bank_import_commit` | authenticated | счёта кандидатов проверяются на принадлежность команде | `can_edit_finance` **или** `is_service_role` | да (Vercel-cron) |
| `bank_sync_claim` | authenticated | по `team_id` + provider | то же | да |
| `is_service_role` | authenticated | — | роль берётся из проверенных claim'ов JWT PostgREST; клиент подменить не может | — |
| `can_modify_tx` | authenticated | команда из операции | NULL-safe | — |
| `op_begin` / `op_finish` | **никто** (revoke all from public) | вызываются только изнутри других функций | — | — |

### Результат атак «команда A → объект команды B»

21 проверка, все PASS: `invoice_save`, `obligation_allocate`, `transaction_split`,
`transaction_save`, `transaction_delete`, `transactions_bulk_patch`,
`transactions_convert_to_transfer`, `transaction_insert`, `bank_import_commit`,
`bank_sync_claim` — отказ; посторонний (не участник) — отказ; `viewer` своей
команды — отказ на запись; `can_modify_tx` — `false` на чужую операцию и `true` на
свою; `is_service_role` — `false` для клиента; `op_begin` — `permission denied`.
Снимок данных команды B (операции / разнесения / позиции / оборот / периоды)
до и после всех попыток **идентичен**.

Ошибка, найденная этим тестом и исправленная до отправки: в
`transactions_bulk_patch` и `transactions_convert_to_transfer` использовался
`min(team_id)` — такой агрегат для `uuid` в Postgres не существует, функция падала
до проверки прав. Заменено на отдельный запрос.

---

## 4. Репетиция миграций (объём production)

Фикстура повторяет объёмы production на 2026-09-22: `transactions` 7 938,
`transaction_history` 6 657, `import_batches` 265, `obligations` 189,
`counterparties` 157, `obligation_payments` 96, `invoices` 25, `accounts` 24,
`invoice_items` 22, `transaction_splits` 4, `project_periods` 3. Суммы и даты
намеренно повторяются, чтобы воспроизвести коллизии отпечатков.

| Миграция | Время |
|---|---|
| 0086_guard_constraints | 0,06 с |
| 0087_accrual_idempotency | 0,06 с |
| 0088_financial_rpcs | 0,07 с |
| 0089_optimistic_concurrency | 0,05 с |
| **0090_bank_event_identity** | **0,42 с** |
| 0091_write_paths_and_authz | 0,06 с |
| **Итого** | **≈ 0,7 с** |

| Параметр | Значение |
|---|---|
| Перезапись таблицы `transactions` | **да** (`relfilenode` меняется) — из-за `ADD COLUMN … GENERATED … STORED` |
| Длительность самого «тяжёлого» ALTER на 7 938 строках | **0,076 с** (это и есть длительность `ACCESS EXCLUSIVE`) |
| Размер `transactions` до / после | 2 280 kB → 5 680 kB (на фикстуре; в проде сейчас 6 936 kB, ожидаемый рост — до ~1,7–2×) |
| Новые индексы | `bank_event_fp` 128 kB, `bank_event_fp_strong` 1 136 kB, `bank_event_id` 288 kB |
| Ошибки при применении | нет |
| Валидация ограничений | `transactions_amount_check`, `transactions_origin_check` — `convalidated = true` |
| Backfill `transactions.origin` | bank 4 763 · bank_csv 1 588 · manual 1 587 |
| Backfill `bank_event_id` | заполнен у 4 763 строк |
| Backfill `obligations.origin` | 189 строк (на фикстуре все `manual`) |
| Коллизии слабого отпечатка на фикстуре | 480 групп |
| Коллизии сильного отпечатка | 120 групп, все с 2+ provider id — и все строки **сохранены** (T14) |
| Целостность после миграций | инвойсы ≠ Σ позиций: 0 · переплат: 0 · дублей номеров: 0 · Σ частей ≠ сумме: 0 · строк в `transactions` 7 938 (не изменилось) |
| Регрессия на этой же БД | T1–T14 **PASS 14 / FAIL 0** |
| Авторизация на этой же БД | **PASS 21 / FAIL 0** |

Оговорка: 7 938 строк — небольшой объём, поэтому абсолютные времена малы.
Перезапись таблицы линейна по объёму; при росте до ~100 тыс. строк ожидается
порядка 1 с, до ~1 млн — порядка 10 с. Окно обслуживания рассчитано с запасом.

---

## 5. Откат

Скрипты: `supabase/migrations/rollback/0086_down.sql … 0091_down.sql` + `README.md`
там же. Проверено: все `*_down.sql` применяются без ошибок на копии БД с
применёнными миграциями, колонок `bank_event*` после откатa — 0, строки
`transactions` не теряются.

| Миграция | Схема совместима со СТАРЫМ кодом | Нужен согласованный откат приложения |
|---|---|---|
| 0086 | да (только новые ограничения) | нет |
| 0087 | да (сигнатуры не менялись) | нет |
| 0088 | да (только новые объекты) | нет |
| 0089 | да (новые колонки + триггеры) | нет |
| 0090 | да (новые объекты; старый дедуп по `source`+`external_id` остаётся) | нет |
| 0091 | да (`can_*` стали строже; сняты лишние гранты) | нет |

**Обратное направление несовместимо:** новый код требует RPC из 0088–0091 и
колонки `transactions.version`. Поэтому деплой — «сначала БД, потом код», а
откат — «сначала код, потом (если нужно) БД». Откатывать миграции при работающем
новом коде нельзя.

Особые случаи:
* `0087_down` не восстанавливает прежние тела `materialize_auto_accruals` /
  `materialize_support_cycles` автоматически — их текст нужно сохранить **до**
  деплоя (`pg_get_functiondef`), иначе откат неполный. Это записано в runbook как
  обязательный шаг PRECHECK.
* `0088_down` удаляет `operation_requests` — после этого повторная отправка тех же
  форм снова может создать дубли.
* `0090_down` удаляет `bank_reconciliation_conflicts` — выгрузить, если конфликты
  уже разбирались.
* Откат 0091 возвращает SEC-008. Делать только вместе с отказом от 0091 целиком.

---

## 6. SPLIT-01 — два слоя учёта: cash и management

### Правило, закреплённое в системе

| Слой | Источник истины | Где живёт | Считается |
|---|---|---|---|
| **CASH** — движение денег | таблица `transactions` | остатки счетов, `account_balances`, сверка с банком, месячные притоки/оттоки | исходная операция **ровно один раз**; разнесение на неё не влияет |
| **MANAGEMENT** — управленческая аналитика | представление `transaction_lines` (миграция 0092) | проекты, сотрудники, подрядчики/контрагенты, статьи, любые другие разрезы | части операции, если они есть; иначе — сама операция одной строкой |

Каноническое правило реализовано **один раз** — в представлении:

```sql
coalesce(s.amount, t.amount), coalesce(s.project_id, t.project_id),
coalesce(s.counterparty_id, t.counterparty_id), coalesce(s.category_id, t.category_id)
from transactions t left join transaction_splits s on s.transaction_id = t.id
```

Отсюда следует главное свойство: **Σ строк одной операции всегда равна её сумме** —
если части есть, их сумма равна сумме операции (это держит deferrable-триггер
`transaction_splits_total_ck` из 0086); если частей нет, строка ровно одна.
Двойной учёт (200 000 вместо 100 000) в этом слое невозможен **по построению**.
Трассировка: `transaction_id` → исходная операция → `bank_event_id` (банковское
событие), `split_id` → конкретная часть.

Представление объявлено `security_invoker = on`, поэтому RLS таблиц применяется
к вызывающему: через него нельзя увидеть операции чужой команды.

> Факт, найденный при реализации: представление `transaction_lines` **уже
> существовало в production** — создано вне миграций, с `security_invoker=on`,
> и использовалось ровно одной страницей (карточка проекта). Миграция 0092
> кодифицирует его, дописывает недостающие колонки (`transaction_amount`,
> `split_id`, `source`, `created_by`, `transfer_account_id`) и снимает лишние
> гранты на запись у `authenticated`/`anon`.

### Инвентаризация consumers и классификация

| Потребитель | Слой | Что сделано |
|---|---|---|
| Карточка проекта `projects/[id]` — суммы | MANAGEMENT | уже читала `transaction_lines` (до этого PR) |
| Карточка проекта — реестр операций | MANAGEMENT → список CASH-строк | **исправлено**: в список попадают операции, связанные с проектом напрямую **или через часть** |
| Карточка сотрудника `employees/[id]` — фактические выплаты | MANAGEMENT | **исправлено**: выплаты берутся из `transaction_lines` |
| Карточка контрагента `counterparties/[id]` — реестр | MANAGEMENT → список CASH-строк | **исправлено**: как у проекта |
| Бюджеты `budgets` — расходы по статьям | MANAGEMENT | **исправлено** |
| Дашборд — структура расходов по статьям за год | MANAGEMENT | **исправлено** |
| Дашборд — месячные доходы/расходы, остатки, прогноз | CASH | без изменений (операция считается один раз) |
| Отчёты `reports` — по статьям, проектам, месяцам | MANAGEMENT | **исправлено** |
| Выгрузка CSV из `reports` | MANAGEMENT | **исправлено**: строка на часть + колонки «Операция» и «Часть» для трассировки |
| ОПиУ `reports/pnl` | MANAGEMENT | части берутся из `transaction_lines` (раньше — свой запрос к `transaction_splits`) |
| ДДС `reports/cashflow` — разбивка по статьям | MANAGEMENT | то же |
| ДДС — месячные ИТОГО и сальдо | CASH | без изменений |
| Счета `accounts`, `account_balances` | CASH | без изменений — это и есть банковский остаток |
| Начисления: `payroll`, `agents`, `employees` (верхний блок), `debts` | ACCRUAL (обязательства) | без изменений. Разнесение фактической оплаты **не** создаёт начислений — это разные экономические события (тест S11) |
| Выплаты `AgentPayouts`, `PayrollRowActions` | CASH + ACCRUAL | `obligation_pay`: операция + разнесение на обязательство одной транзакцией |
| Импорт, сверка, реестр операций, карточка операции, правила, планируемые | CASH | без изменений — работают с операцией как с денежным движением |
| `licenses` — операции по «лицензионным» статьям | CASH-список | без изменений: страница показывает операции, а не аналитику по разрезам |
| `transactions/categorize`, `/sort` | CASH-редактирование | без изменений; операция с частями, но без своей статьи, по-прежнему считается «без статьи» — отдельная UX-задача |

### Обязательный сценарий — 36 из 36 PASS

`100 000 → 60/40 (разные проекты, исполнители, статьи) → 25/75 → снять разнесение
→ повторный синк банка → параллельная правка`:

| Инвариант | pre | post |
|---|---|---|
| `SUM(split parts) = original amount` | PASS | **PASS** |
| Проект A = 60 000 / B = 40 000, затем 25 000 / 75 000 | PASS | **PASS** |
| Исполнитель A / B, Статья A / B — соответствующие части | PASS | **PASS** |
| `Management P&L total = 100 000` (не 200 000) | PASS | **PASS** |
| `Cash movement = 100 000 ровно один раз` (остаток счёта) | PASS | **PASS** |
| Правка 60/40 → 25/75: старых значений нигде не осталось | PASS | **PASS** |
| Снятие разнесения → fallback к операции на 100 000 | PASS | **PASS** |
| Повторный bank sync не меняет разнесение | PASS | **PASS** |
| Изменение split атомарно | — | **PASS** |
| Concurrent edit → conflict, без silent overwrite | **FAIL** (4 части, 200 000) | **PASS** |
| Части ≠ сумме операции не сохраняются | **FAIL** | **PASS** |
| Разнесение не создаёт второго начисления/обязательства | PASS | **PASS** |
| Потребители (проект, сотрудник, статья, отчёты, реестр) | — | **PASS** |
| `Σ строк = сумме операции` для КАЖДОЙ операции | — | **PASS** |

### BEFORE / AFTER на реальных данных production

Две существующие операции с разнесением (прочитаны из production `SELECT`'ом,
воспроизведены в disposable-БД; `scripts/concurrency/split_prod_replay.sh`):

* `42d431aa…` — расход 50,00 USDT, контрагент Asana, статья «Сервисы/связь»,
  части 26,00 + 24,00 с теми же измерениями;
* `b7457804…` — расход 200,00 USDT, **у самой операции нет ни проекта, ни
  контрагента**, части 100,00 → проект [125] Re:clinic и 100,00 → проект [126]
  Maison, обе на исполнителя «Станислав Кутишко».

| Потребитель / разрез | BEFORE | AFTER |
|---|---:|---:|
| Проект [125] Re:clinic | 0,00 | **100,00 USDT** |
| Проект [126] Maison development | 0,00 | **100,00 USDT** |
| Сотрудник «Станислав Кутишко» | **0,00** | **200,00 USDT** |
| Контрагент «Asana» | 50,00 | 50,00 |
| Статья «Выплата % / бонусы за проект» | 200,00 | 200,00 |
| Статья «Сервисы/связь» | 50,00 | 50,00 |
| **ИТОГО расходы (ОПиУ / ДДС / дашборд)** | **250,00** | **250,00** |
| Остаток счёта | −250,00 | −250,00 |

BEFORE здесь — прежнее правило агрегации (по полям самой операции); именно так
считали карточка сотрудника, бюджеты, дашборд и отчёты. Карточка проекта уже
читала `transaction_lines`, поэтому по проектам её реальный BEFORE совпадал с
AFTER — в таблице показано правило, а не поведение конкретной страницы.

Главное: выплата 200,00 USDT сотруднику, которая сейчас **не видна** в его
карточке (у операции не заполнен контрагент — он есть только у частей), после
изменения отображается полностью и разложена по двум проектам. При этом итог и
остаток счёта не изменились — двойного счёта нет.

### Поправка к прошлому отчёту

В предыдущем сообщении я написал, что карточка проекта не учитывает части.
Это **неверно**: `projects/[id]/page.tsx` уже считал суммы через
`transaction_lines`. Неполным был список операций на этой странице, а суммы —
корректными. Ошибочными по суммам были карточка сотрудника, карточка
контрагента, бюджеты, дашборд и отчёты — они и исправлены.

## 7. GO / NO-GO

| Миграция | Решение | Причина |
|---|---|---|
| **0086_guard_constraints** | **GO** | Только новые ограничения; текущих нарушений в проде 0 по всем четырём инвариантам (проверено). Применение 0,06 с, без перезаписи таблиц. Закрывает T2/T4/T5/CONC-08 на уровне схемы. Совместима со старым кодом. |
| **0087_accrual_idempotency** | **GO** | Закрывает T1 (двойное начисление ЗП). Backfill `obligations.origin` консервативный (189 строк, финансовые поля не меняются). Сигнатура функции сохранена. Требование runbook: до деплоя сохранить прежние тела двух функций для отката. |
| **0088_financial_rpcs** | **GO** | Только новые таблицы и функции, ничего существующего не меняет. Права проверены 21 тестом; `op_begin`/`op_finish` клиенту не выданы. Закрывает T2/T3/T4/T5/T9. |
| **0089_optimistic_concurrency** | **GO** | `ADD COLUMN NOT NULL DEFAULT 1` без перезаписи таблицы. Закрывает T8 и делает правку операции и её частей одной транзакцией. Старый код совместим. |
| **0090_bank_event_identity** | **GO** (версия 2) | Первая версия была **NO-GO**: collision analysis показал 446 групп отпечатка с разными id провайдера, то есть сценарий silent drop реальной операции. Переписано: пропуск возможен только по id провайдера или по однозначному 1:1 совпадению сильного отпечатка, любой пропуск журналируется, неоднозначность → вставка + conflict. T12/T13/T14 подтверждают. Требует окна: перезапись `transactions` (на текущем объёме < 0,1 с). |
| **0092_transaction_lines** | **GO** | Кодифицирует канонический слой управленческой аналитики (SPLIT-01). Только представление: данные не меняются, применение 0,04 с, блокировок нет. `security_invoker = on` — RLS применяется к вызывающему. Инвариант «Σ строк = сумме операции» проверен и на синтетике, и на живых данных production (0 нарушений). Откат — `drop view`, приложение при этом надо откатить первым. |
| **0091_write_paths_and_authz** | **GO** | Исправляет **SEC-008** (HIGH, существует в проде: NULL-семантика `can_*` пропускает не-участника в 6 SECURITY DEFINER функциях, две из них с грантом anon/PUBLIC пишут финансовые данные — воспроизведено на копии). Закрывает обходы CAS (`RulesManager`, `PlannedReview`, массовые правки) и гонку материализации регулярных операций. Строго более ограничивающая семантика. |

### Общий статус: **GO** для применения 0086–0092 после подтверждения

Проверка условия заказчика «если существует хотя бы один сценарий silent
loss/duplication финансовой операции — общий статус NO-GO»:

| Сценарий | Статус после PR #99 |
|---|---|
| Потеря операции при повторном/параллельном импорте | закрыто: кандидат с id провайдера не пропускается никогда; T6/T12/T14 |
| Дублирование события двумя импортёрами | закрыто: `bank_event_id` + сверка по отпечатку; T10/T11/T13 |
| Молчаливый пропуск кандидата без id провайдера | **не молчаливый**: каждое такое решение пишется в `bank_reconciliation_conflicts` с полным payload и обратимо |
| Двойной учёт денег при split/склейке | закрыто: T9, S7 |
| Двойное начисление ЗП | закрыто: T1 |
| Потеря чужой правки (lost update) | закрыто: T8, S7 + `transactions_bulk_patch` |
| Потеря ручного разнесения при синке | закрыто: S6 |
| Стирание разнесения быстрым «Сохранить» | закрыто (правка `collect()`) |
| Неправильные суммы в управленческой аналитике при разнесении | закрыто: SPLIT-01, единый слой `transaction_lines`, 36/36 тестов + BEFORE/AFTER на живых данных |

**Остаточные риски, не блокирующие деплой** (все зафиксированы выше):
`licenses` и `transactions/categorize|sort` работают с операцией целиком
(осознанно: это списки и редактирование операций, а не аналитика по разрезам);
`bybit_ingest` (жёстко прошитые team/account, дедуп без уникального индекса),
`PlannedReview` авто-закрытие и массовое удаление (обоснованы),
CSV-импорт без id провайдера принципиально не различает «то же событие» и «вторую
такую же операцию» — решение журналируется.

**Не входит в этот PR и требует отдельного согласования:** историческая чистка
FIN-03 (126 пар, 252 строки) и решение по `opening_balance`; шаги 7–9 плана
(валюта FIN-01/FIN-02, SEC-001/SEC-002, CI-гейты).
